linux special permissions
suid: setting suid on a file allows any user to run a file as root even if that file wasnt created by that user
two ways by which file permissions can be altered are:
symbollic method:$chmod whowhatwhich file | directory
who- represents identities: u,g,o, a(user, group, all, other)
what- represents identities: u,g,o, a(user, group, all, other)

numeric method:

